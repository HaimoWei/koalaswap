server:
  port: ${SERVER_PORT:12651}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://db:5432/koalaswap_dev}
    username: ${SPRING_DATASOURCE_USERNAME:koalaswap}
    password: ${SPRING_DATASOURCE_PASSWORD:secret}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
    connect-retries: 5
    postgresql-compatibility: "15"
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:redis}
      port: ${SPRING_DATA_REDIS_PORT:6379}

app:
  token-freshness:
    cache-ttl-sec: ${APP_TOKEN_FRESHNESS_TTL_SEC:8}
    use-redis: ${APP_TOKEN_FRESHNESS_USE_REDIS:true}
    pubsub-channel: ${APP_TOKEN_FRESHNESS_CHANNEL:auth:pv:changed}
  jwt:
    secret: ${APP_JWT_SECRET:}
    issuer: ${APP_JWT_ISSUER:koalaswap}
    access-token-minutes: ${APP_JWT_ACCESS_MINUTES:60}
  order-events:
    enabled: ${APP_ORDER_EVENTS_ENABLED:true}
    channel: ${APP_ORDER_EVENTS_CHANNEL:orders:completed}
  services:
    user:
      base-url: ${APP_USER_SERVICE_EXTERNAL_BASE_URL:http://user-service:8080}
    product:
      base-url: ${APP_PRODUCT_SERVICE_EXTERNAL_BASE_URL:http://product-service:8080}
  user-service:
    internal-base-url: ${APP_USER_SERVICE_INTERNAL_BASE_URL:http://user-service:8080}
  product-service:
    internal-base-url: ${APP_PRODUCT_SERVICE_INTERNAL_BASE_URL:http://product-service:8080}

logging:
  level:
    com.koalaswap.review: ${LOGGING_LEVEL_COM_KOALASWAP_REVIEW:INFO}
    com.koalaswap.common.security: ${LOGGING_LEVEL_COM_KOALASWAP_COMMON_SECURITY:INFO}
    org.springframework.security: ${LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY:INFO}
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
