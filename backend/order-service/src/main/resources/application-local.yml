# 本地开发 profile=local
server:
  port: 12650

spring:
  datasource:
    url: jdbc:postgresql://localhost:15433/koalaswap_dev
    username: koalaswap
    password: secret
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.jdbc.time_zone: UTC

  # 如需 Redis + Pub/Sub（和 product-service 一致）
  data:
    redis:
      host: localhost
      port: 16379

app:
  # ===== JWT（本地给出明确的值，避免占位符报错）=====
  jwt:
    secret: "p9P2JYpN4mVJXWm7XyQw6kBsF3uCz7rGQh5M8tQ2nA1rS6vY0kL9dH4eT8uZ3xB1"
    issuer: koalaswap
    access-token-minutes: 60

  product-service:
    internal-base-url: http://localhost:12648
  user-service:
    internal-base-url: http://localhost:12649

  token-freshness:
    cache-ttl-sec: 8
    use-redis: true         # 如果你本机没起 redis，临时改为 false 也可以
    pubsub-channel: auth:pv:changed

logging:
  level:
    com.koalaswap.common.security: DEBUG
    org.springframework.security: INFO
    com.koalaswap.order.security: DEBUG
